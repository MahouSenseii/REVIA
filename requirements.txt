# REVIA - Combined Requirements
# Install all dependencies: pip install -r requirements.txt

# === Core Server (revia_core_py) ===
flask>=3.0.0
flask-cors>=4.0.0
websockets>=12.0

# === Controller GUI (revia_controller_py) ===
PySide6>=6.6.0
requests>=2.31.0
opencv-python>=4.8.0
SpeechRecognition>=3.10.0
PyAudio>=0.2.14
pyttsx3>=2.90
sounddevice>=0.4.6
numpy>=1.24.0

# === Platform Integrations ===
discord.py>=2.3.0      # Discord bot — pip install "discord.py"
twitchio>=2.8.0        # Twitch IRC bot — pip install twitchio

# === Web Search (optional — enables real-time internet lookups) ===
duckduckgo-search>=6.0.0  # Free, no API key — pip install duckduckgo-search

# === Optional dependencies ===
psutil>=5.9.0
gradio_client>=0.14.0
soundfile>=0.12.0
