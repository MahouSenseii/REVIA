# REVIA optional PyTorch CUDA 12.8 install
#
# Use this on RTX 50-series / sm_120 GPUs when older PyTorch CUDA wheels fail
# with "no kernel image is available" or TORCH_USE_CUDA_DSA messages.
#
# Install after the base requirements:
#   .\.venv\Scripts\python.exe -m pip install -r requirements-torch-cu128.txt
#
# Keep this separate from requirements.txt so older CUDA, CPU-only, and existing
# working PyTorch installs are not forced to replace their torch build.

--index-url https://download.pytorch.org/whl/cu128
torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0
